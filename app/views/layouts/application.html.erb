<!DOCTYPE html>
<html>
  <head>
    <title>WsR! | Find Jobs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Train+One&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= what_nav(request) %>
    <%= greet(request) %>
    <div class="p-2 md:p-8 w-100 h-100">
      <div class="grid grid-cols-1 gap-y-2 gap-x-4">
        <%= yield %>
      </div>
    </div>
    <% if false %>
    <% if !request.path.include?('/jobs/new') %>
      <div class="footer booster sticky bottom-0">
        <div class="copyright text-center p-6">© <%= Time.zone.now.year %> worksite.rocks - All Rights Reserved</div>
      </div>
    <% end %>
    <% end %>
  </body>
</html>
